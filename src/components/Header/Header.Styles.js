
import styled from 'styled-components'

export const HeaderWrapper = styled.header`
  background-color: var(--color-main-tp);
  color: var(--color-accent);
  padding: 1.3rem 1.8rem;

  h1{
      text-align: center;
      margin: 0 auto;
      font-size: 1.5rem; 
  }  
`;
